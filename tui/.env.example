# Local dev — copy to .env, fill in the password from docker-compose's POSTGRES_PASSWORD.
# Required by sqlx::query!() macros at compile time; they verify SQL against this live DB.
DATABASE_URL=postgres://prospecta:CHANGEME@localhost:5432/prospecta
